# Core
datasets>=2.18           # CommonsenseQA loading (tau/commonsense_qa)
pandas>=2.0
numpy>=1.24
pyyaml>=6.0              # config loading
tqdm>=4.66
python-dotenv>=1.0       # load API keys from .env

# Stats / plotting (analysis)
scipy>=1.11
statsmodels>=0.14        # McNemar paired significance test
matplotlib>=3.7

# Translation backend (Google Cloud Translation v2 REST — API key in GOOGLE_API_KEY)
requests>=2.31

# Generative arm — vLLM-served models via the OpenAI-compatible client
openai>=1.40

# Encoder arms — XLM-R (fine-tuned) and ESIM (anchor)
torch>=2.2
transformers>=4.40
sentencepiece>=0.2       # XLM-R tokenizer

# Dev
pytest>=8.0
ruff>=0.4
